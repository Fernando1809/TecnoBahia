rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Nunca expira y requiere inicio de sesión
      allow read, write: if request.auth != null;
    }
  }
}